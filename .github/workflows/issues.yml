name: GitHub Issues
on: [issues]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 The github issue info ${{ toJSON(github.event.issue) }}."
